<div class="container">
  <h1>Movie lists : </h1>
  <% @lists.each do |list| %>
  <div class='card'>
    <p><%=link_to list.name, list_path(list.id) %></p>
  </div>
  <% end %>
  <h3><%= link_to "+Add list", new_list_path %></h3>
</div>
